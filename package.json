{
  "name": "trix-sdk-python",
  "version": "0.1.1",
  "private": true,
  "description": "Development tooling for trix-sdk-python",
  "devDependencies": {
    "@commitlint/cli": "^18.4.3",
    "@commitlint/config-conventional": "^18.4.3"
  },
  "scripts": {
    "commitlint": "commitlint --edit"
  }
}
